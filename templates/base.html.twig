<!DOCTYPE html>
<html lang="fr-FR">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}{{ site_name }} - Tatouage à Aniane (34){% endblock %}</title>
        <link rel="icon" href="{{ asset('favicon.ico') }}">
        <link rel="canonical" href="{{ app.request.uri }}">

        {% block seo %}
            <meta name="description" content="{% block meta_description %}Brume d'Encre Tattoo : Mona, tatoueuse à Aniane (34150). Tatouages fins, floraux et aquarelle. Shop privé sur rendez-vous près de Montpellier.{% endblock %}">
            <meta name="keywords" content="tatouage, tattoo, tatoueuse Aniane, tatoueur, tatoueuse, tatoueuse Hérault, fine line, floral, aquarelle, Brume d'Encre, Mona tattoo, salon tatouage 34">

            <!-- Open Graph / Facebook -->
            <meta property="og:type" content="website">
            <meta property="og:url" content="{{ app.request.uri }}">
            <meta property="og:title" content="{{ block('title') }}">
            <meta property="og:description" content="{{ block('meta_description') }}">
            <meta property="og:image" content="{{ absolute_url(asset('images/home-image.webp')) }}">

            <!-- Twitter -->
            <meta property="twitter:card" content="summary_large_image">
            <meta property="twitter:url" content="{{ app.request.uri }}">
            <meta property="twitter:title" content="{{ block('title') }}">
            <meta property="twitter:description" content="{{ block('meta_description') }}">
            <meta property="twitter:image" content="{{ absolute_url(asset('images/home-image.webp')) }}">

            <script type="application/ld+json">
            {
                "@context": "https://schema.org",
                "@type": "LocalBusiness",
                "name": "{{ site_name }}",
                "image": "{{ absolute_url(asset('images/home-image.webp')) }}",
                "@id": "{{ url('app_home') }}",
                "url": "{{ url('app_home') }}",
                "telephone": "",
                "address": {
                    "@type": "PostalAddress",
                    "streetAddress": "47 Boulevard Saint-Jean",
                    "addressLocality": "Aniane",
                    "postalCode": "34150",
                    "addressCountry": "FR"
                },
                "geo": {
                    "@type": "GeoCoordinates",
                    "latitude": 43.6841,
                    "longitude": 3.5857
                },
                "openingHoursSpecification": {
                    "@type": "OpeningHoursSpecification",
                    "dayOfWeek": [
                        "Tuesday",
                        "Wednesday",
                        "Thursday",
                        "Friday",
                        "Saturday"
                    ],
                    "note": "Sur rendez-vous uniquement"
                },
                "sameAs": [
                    "https://www.instagram.com/brumedencre_tattoo/",
                    "https://www.facebook.com/monatatoueuse"
                ]
            }
            </script>
        {% endblock %}

        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark">
            <div class="container-fluid">
                <a class="navbar-brand d-flex align-items-center" href="{{ path('app_home') }}">
                    <img src="{{ asset('images/nav-brand.webp') }}" alt="BrumeEncreTattoo" width="70" class="d-inline-block align-text-top">
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="{{ path('app_home') }}">Accueil</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_portfolio')}}">Galerie</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_contact')}}">Contact</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>

        <div class="mt-4">
            {% block body %}{% endblock %}
        </div>

        <footer class="text-center py-4 bg-light">
            <div class="container">
                <div class="row">
                    <div class="col-md-4 mb-3 mb-md-0">
                        <h5>{{ site_name }}</h5>
                        <p class="small text-muted">Shop privé de tatouage à Aniane (34). Sur rendez-vous uniquement.</p>
                    </div>
                    <div class="col-md-4 mb-3 mb-md-0">
                        <h5>Liens utiles</h5>
                        <ul class="list-unstyled">
                            <li><a href="{{ path('app_home') }}" class="text-decoration-none text-dark">Accueil</a></li>
                            <li><a href="{{ path('app_portfolio') }}" class="text-decoration-none text-dark">Galerie Portfolio</a></li>
                            <li><a href="{{ path('app_contact') }}" class="text-decoration-none text-dark">Contact & RDV</a></li>
                        </ul>
                    </div>
                    <div class="col-md-4">
                        <h5>Suivez-moi</h5>
                        <div class="social-links">
                            <a href="https://www.instagram.com/brumedencre_tattoo/?hl=fr" target="_blank" title="Instagram" class="me-2"><i class="fab fa-instagram fa-lg"></i></a>
                            <a href="https://www.facebook.com/monatatoueuse" target="_blank" title="Facebook"><i class="fab fa-facebook fa-lg"></i></a>
                        </div>
                    </div>
                </div>
                <hr>
                <p class="mb-0 small">&copy; {{ "now"|date("Y") }} {{ site_name }}. Tous droits réservés. Tatoueuse à Aniane, Hérault.</p>
            </div>
        </footer>
    </body>
</html>
