{% extends 'base.html.twig' %}

{% block title %}Galerie Tatouages : Fine line, Floral et Dentelle à Aniane - Brume d'Encre Tattoo{% endblock %}

{% block seo %}
    <meta name="description" content="{% block meta_description %}Explorez la galerie de Brume d'Encre Tattoo à Aniane. Découvrez nos réalisations de tatouages fins, floraux, dentelles et aquarelles sur mesure.{% endblock %}">
    {{ parent() }}
{% endblock %}

{% block body %}
<div class="container py-5">
    <h1 class="text-center mb-5">Galerie Portfolio</h1>

    <div class="row">
        {% for image in images %}
            <div class="col-md-4 mb-4">
                <div class="portfolio-card shadow-sm h-100">
                    <img src="{{ asset('images/portfolio/' ~ image.src) }}"
                         alt="{{ image.alt }}"
                         class="img-fluid rounded w-100"
                         loading="lazy">
                </div>
            </div>
        {% endfor %}
    </div>

    <div class="text-center mt-5">
        <p class="lead">Chaque pièce est unique et réalisée sur mesure.</p>
        <a href="{{ path('app_contact') }}" class="btn btn-primary btn-lg">Discutons de ton projet</a>
    </div>
</div>
{% endblock %}
